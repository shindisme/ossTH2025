on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
      
    - name: 📂 Sync files
      # Định nghĩa env ở cấp độ STEP
      env:
        FTP_HOST: ${{ secrets.FTP_HOST }}
        FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
        FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        DEPLOY_PATH: /var/www/html/
      
      # Ví dụ dùng run để kiểm tra biến
      run: |
        echo "Deploy đến $FTP_HOST tại $DEPLOY_PATH"
        # Sau đó, bạn dùng các biến này trong 'uses' bên dưới
        # uses: SamKirkland/FTP-Deploy-Action@v4.3.1
        # với:
        #   server: ${{ env.FTP_HOST }} # Hoặc trực tiếp dùng secrets/env
        # ...
